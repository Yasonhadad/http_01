// Local runtime configuration.
// Fill with real values or keep empty to use fallback sample feed.
window.THE_ONE_CONFIG = {
    supabaseUrl: "",
    supabaseAnonKey: "",
    userId: "",
    crmSync: true,
    feedLimit: 80
};
